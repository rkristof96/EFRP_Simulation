output<-function(){
  output <- list()
  output[["rmse_resultslist"]]<-list()
  # modelsfrequencylist: count how many times were lower the RMSE
  output[["modelsfrequencylist"]] <-list()
  
  return(output)
}